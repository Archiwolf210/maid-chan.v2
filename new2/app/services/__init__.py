"""Domain services. Distinct from `utils/` (pure helpers): each module here
encapsulates a slow-evolving subsystem with its own state model.

  key_memories  -- anchor-moment detection + slow trait evolution
"""
